---
"@klappay/cli": minor
---

Bump `@klappay/node` to `4.2.0`. Adds `klap charges watch <id>`, a live tail of a single charge's status and confirmation progress straight from its own event stream — no webhook endpoint or `klap listen` relay needed.
